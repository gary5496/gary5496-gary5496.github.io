<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title>Github homepage setup with Hexo in Windows</title>
      <link href="/2018/03/06/hexo-github-setup/"/>
      <content type="html"><![CDATA[<p>If you want to set up a Github homepage with Hexo, and you need your homepage set up with basic functionality, like tags, categories, about, search, theme and etc, you can follow the below guide.</p><h2 id="Steps"><a href="#Steps" class="headerlink" title="Steps"></a>Steps</h2><h3 id="1-Set-up-the-account-and-repository-in-GitHub"><a href="#1-Set-up-the-account-and-repository-in-GitHub" class="headerlink" title="1. Set up the account and repository in GitHub"></a>1. Set up the account and repository in GitHub</h3><p>Create an account in <a href="https://github.com/" target="_blank" rel="noopener">https://github.com/</a>, click on the <em>Create New</em> button in the left upper corner, choose <em>New Repository</em>. </p><p>In the <em>Repository name</em>, input <em><your github="" account\="">.github.io</your></em> such as “gary5496.github.io”, select the checkbox <em>Initialize this repository with a README</em>, and click on button <em>Create repository</em>.</p><p>In the home page of the new repository, choose tab <em>Settings</em>, in the <em>GitHub Pages</em> section, change <em>Source</em> from <em>None</em> to <em>master branch</em>, and click on <em>Save</em> button next to it.</p><h3 id="2-Install-Git"><a href="#2-Install-Git" class="headerlink" title="2. Install Git"></a>2. Install Git</h3><p>Git is the tool to manage your repository on GitHub.</p><p>Download the Git from <a href="https://git-scm.com/download/win" target="_blank" rel="noopener">https://git-scm.com/download/win</a>, and install it.</p><p>After installation, right click on any folder, and choose <em>Git Bash Here</em> to open Git Bash. To bind your account and email to your local computer, run the below command.</p><pre><code>git config --global user.name &quot;your GitHub account&quot;git config --global user.email &quot;your GitHub email address&quot;</code></pre><h3 id="2-Install-Node-js"><a href="#2-Install-Node-js" class="headerlink" title="2. Install Node.js"></a>2. Install Node.js</h3><p>Hexo is a framework based on Node.js.</p><p>Download Node.js from <a href="https://nodejs.org/en/download/" target="_blank" rel="noopener">https://nodejs.org/en/download/</a>, and install it. Run the below command to validate the installation status after installation.</p><pre><code>node -v</code></pre><p>npm is the package manager for Node.js. Run the below command to check the npm status.</p><pre><code>npm -v</code></pre><h3 id="3-Install-Hexo"><a href="#3-Install-Hexo" class="headerlink" title="3. Install Hexo"></a>3. Install Hexo</h3><p>Hexo is a framework which can be used to build your blog. See <a href="https://hexo.io/" target="_blank" rel="noopener">https://hexo.io/</a> for details.</p><p>Create a folder as the base folder for Hexo installation. Run CMD command from the Hexo folder, and run the below command to install Hexo.</p><pre><code>npm install -g hexo-cli </code></pre><p>Run the below command to install Hexo Git deployer</p><pre><code>npm install hexo-deployer-git --save</code></pre><p>Run the below command to initialize Hexo</p><pre><code>hexo init</code></pre><p>Run the below command to start Hexo server. You can stop the server via Ctrl + C.</p><pre><code>hexo server</code></pre><p>After the server start, open the below link in browser, and you can see the initial page for Hexo</p><pre><code>http://localhost:4000/</code></pre><h3 id="4-Hexo-basic-commands"><a href="#4-Hexo-basic-commands" class="headerlink" title="4. Hexo basic commands"></a>4. Hexo basic commands</h3><p>Initialize Hexo after installation</p><pre><code>hexo init</code></pre><p>Start Hexo server</p><pre><code>hexo serverhexo s</code></pre><p>Create a new page</p><pre><code>hexo new page &lt;page_name&gt;</code></pre><p>Create a new post</p><pre><code>hexo new &lt;post_name&gt;hexo n &lt;post_name&gt;</code></pre><p>Generate the HTML pages for md file</p><pre><code>hexo generatehexo g</code></pre><p>Deploy the Hexo contents to GitHub</p><pre><code>hexo deployhexo d</code></pre><p>Generate the HTML page for md file and deploy the contents to GitHub</p><pre><code>hexo deploy --generatehexo d --g</code></pre><p>Clean the cache. It may be necessary after change on page or configuration file.</p><pre><code>hexo clean</code></pre><h3 id="5-Hexo-folder-structure"><a href="#5-Hexo-folder-structure" class="headerlink" title="5. Hexo folder structure"></a>5. Hexo folder structure</h3><ul><li>node_modules: the folder to save Node.js modules for Hexo</li><li>themes: the folder for themes</li><li>source: the folder to save the source markdown (.md) files</li><li>scaffolds: the folder for templates</li><li>public: the folder to save the HTML pages after generation</li><li>_config.yml: the root configuration file</li><li>themes/&lt;theme_name>/_config.yml: the theme configuration file</li><li>source/_posts: the folder to save the post markdown (.md) files</li></ul><h3 id="6-Change-Hexo-theme"><a href="#6-Change-Hexo-theme" class="headerlink" title="6. Change Hexo theme"></a>6. Change Hexo theme</h3><p>Check the Hexo themes from <a href="https://hexo.io/themes/" target="_blank" rel="noopener">https://hexo.io/themes/</a>. </p><p>For example, if you want to download the Next theme and use it, follow the below steps.</p><p>a. Run the below command to download the Next theme to themes folder.</p><pre><code>git clone https://github.com/theme-next/hexo-theme-next themes/next</code></pre><p>b. Change the theme parameter to next in the root configuration file _config.yml</p><pre><code># Extensions## Plugins: https://hexo.io/plugins/## Themes: https://hexo.io/themes/theme: next</code></pre><p>c. Change the parameters in theme configuration file themes/next/_config.yml if necessary.</p><pre><code># Schemes#scheme: Muse#scheme: Mist#scheme: Piscesscheme: Gemini# Social Links.# Usage: `Key: permalink || icon`# Key is the link label showing to end users.# Value before `||` delimeter is the target permalink.# Value after `||` delimeter is the name of FontAwesome icon. If icon (with or without delimeter) is not specified, globe icon will be loaded.social:  GitHub: https://github.com/gary5496 || github  E-Mail: mailto:gary5496@163.com || envelope</code></pre><h3 id="7-About-Markdown"><a href="#7-About-Markdown" class="headerlink" title="7. About Markdown"></a>7. About Markdown</h3><p>Markdown is a text-to-HTML formatting syntax for web writers. Markdown allows you to write using an easy-to-read, easy-to-write plain text format, then convert it to structurally valid XHTML (or HTML). See <a href="https://www.appinn.com/markdown/" target="_blank" rel="noopener">https://www.appinn.com/markdown/</a> for Markdown syntax details.</p><p>Here are some useful syntax.</p><pre><code>*italic text***bold text*****&lt;http://www.url.com&gt;###header 3- list item 1- list item 2text before `small code block` text after![Alt text](/path/to/img.jpg)    code block line 1    code block line 2</code></pre><p>You can choose a Markdown editor like <strong>MarkdownPad</strong> to edit Markdown files. Download it from <a href="http://www.markdownpad.com/" target="_blank" rel="noopener">http://www.markdownpad.com/</a> and install it. The editor has a preview functionality which is very helpful for editing.</p><p>Install <strong>Awesomium 1.6.6 SDK</strong> from <a href="http://markdownpad.com/download/awesomium_v1.6.6_sdk_win.exe" target="_blank" rel="noopener">http://markdownpad.com/download/awesomium_v1.6.6_sdk_win.exe</a> and restart the computer, if LivePreview is not working for MarkdownPad and the error message is <em>This view has crashed!</em></p><h3 id="8-Link-to-a-local-image"><a href="#8-Link-to-a-local-image" class="headerlink" title="8. Link to a local image"></a>8. Link to a local image</h3><p>Update the root configuration file _config.yml. After the configuration change, there will be a folder created when using command <code>hexo new &lt;post_name&gt;</code> to create a new post, and the folder name will be the same as post name.</p><pre><code>post_asset_folder: true</code></pre><p>You can copy your image file to the post folder, and use the below Markdown syntax to add an image in a post. With this approach, the image will be showing in the post, but it is not showing in the home page.</p><pre><code>![alt_text](image_name.jpg)</code></pre><p>You can install the below package and then use the below Markdown syntax to add an image to a post, and the image will be showing in both the post and home page.</p><pre><code>npm install https://github.com/CodeFalling/hexo-asset-image --save{% asset_img image_name.jpg This is an image %}</code></pre><h3 id="9-Add-tags-categories"><a href="#9-Add-tags-categories" class="headerlink" title="9. Add tags, categories"></a>9. Add tags, categories</h3><p>Update the root configuration file.</p><pre><code># Directorysource_dir: sourcepublic_dir: publictag_dir: tagsarchive_dir: archivescategory_dir: categories</code></pre><p>Update the theme configuration file.</p><pre><code>menu:  home: / || home  about: /about/ || user  tags: /tags/ || tags  categories: /categories/ || th</code></pre><p>Run the below command to create the page for tags</p><pre><code>hexo new page tags</code></pre><p>Update the file <em>\source\tags\index.md</em></p><pre><code>---title: tagsdate: 2018-03-06 14:38:05type: &quot;tags&quot;comments: false---</code></pre><p>Run the below command to create the page for categories</p><pre><code>hexo new page categories</code></pre><p>Update the file <em>\source\categories\index.md</em></p><pre><code>---title: categoriesdate: 2018-03-06 14:39:05type: &quot;categories&quot;comments: false---</code></pre><h3 id="10-Add-about-me"><a href="#10-Add-about-me" class="headerlink" title="10. Add about me"></a>10. Add about me</h3><p>Update the theme configuration file.</p><pre><code>menu:  home: / || home  about: /about/ || user</code></pre><p>Run the below command to create the page for about me</p><pre><code>hexo new page about</code></pre><p>Update the file <em>\source\about\index.md</em></p><pre><code>---title: about medate: 2018-03-06 14:19:41---### About meThis is my blog.</code></pre><h3 id="11-Add-search-functionality"><a href="#11-Add-search-functionality" class="headerlink" title="11. Add search functionality"></a>11. Add search functionality</h3><p>Add the below parameters to root configuration file.</p><pre><code>search:  path: search.xml  field: post</code></pre><p>Change the parameters in theme configuration file themes/next/_config.yml.</p><pre><code># Local search# Dependencies: https://github.com/theme-next/hexo-generator-searchdblocal_search:  enable: true</code></pre><h3 id="11-Create-a-new-post"><a href="#11-Create-a-new-post" class="headerlink" title="11. Create a new post"></a>11. Create a new post</h3><p>Run the below command to create a new post</p><pre><code>hexo new &lt;post_name&gt;</code></pre><p>Update the file <em>\source_post\&lt;post_name&gt;.md</em></p><pre><code>---title: Hello Worlddate: 2018-03-06 14:19:41tags: [Windows]categories: Windows---###HelloHello! This is my first blog.    printf(&quot;Hello world!&quot;);</code></pre><h3 id="12-Deploy-to-GitHub"><a href="#12-Deploy-to-GitHub" class="headerlink" title="12. Deploy to GitHub"></a>12. Deploy to GitHub</h3><p>If you are using <strong>HTTP</strong> to deploy your Hexo contents to GitHub, you may get a Get GUI popup to input your GitHub account and password when using the below Git command during deployment.</p><pre><code>hexo deploy</code></pre><p>In this case, the root <em>_config.yml</em> file is configured as below.</p><pre><code>deploy:      type: git      repo: git@github.com:gary5496/gary5496.github.io.git      branch: master</code></pre><p>If you are using <strong>SSH</strong> to deploy your Hexo contents to GitHub, you can follow the below steps.</p><p>a. Run the below command via Git Bash</p><pre><code>ssh-keygen -t rsa -C &quot;your GitHub email address&quot;</code></pre><p>b. Use the default file path <em>C:/User/<windows account\="">/.ssh/id_rsa</windows></em>, and input the passphrase if necessary</p><p>c. Open the file <em>C:/User/<windows account\="">/.ssh/id_rsa.pub</windows></em>, and copy the entire file content</p><p>d. Log on to GitHub, click on <em>Avatar</em> on the left upper corner, choose <em>Settings</em>, choose <em>SSH and GPG Keys</em>, click on button <em>New SSH Key</em>, input the <em>Title</em> with any value, paste the entire file content from last step as <em>Key</em>, and click on button <em>Add SSH Key</em></p><p>e. If the passphrase is not empty in the step b, and you don’t want to reenter your passphrase every time you use your SSH key, you can add your key to the SSH agent, which manages your SSH keys and remembers your passphrase.</p><pre><code>eval $(ssh-agent -s)ssh-add ~/.ssh/id_rsa</code></pre><p>f. Run the below command via Git Bash to check the SSH connection</p><pre><code>ssh -T git@github.com</code></pre><p>The successful result should be like below.</p><pre><code>Hi gary5496! You&apos;ve successfully authenticated, but GitHub does not provide shell access.</code></pre><p>g. Run the below command for deployment</p><pre><code>hexo deploy</code></pre><p>In this case, the root <em>_config.yml</em> file is configured as below.</p><pre><code>deploy:    type: git    repo: https://github.com/gary5496/gary5496.github.io.git    name: gary5496    email: gary5496@163.com    branch: master</code></pre><h3 id="13-Errors-amp-Tips"><a href="#13-Errors-amp-Tips" class="headerlink" title="13. Errors &amp; Tips"></a>13. Errors &amp; Tips</h3><h3 id="14-Links-amp-References"><a href="#14-Links-amp-References" class="headerlink" title="14. Links &amp; References"></a>14. Links &amp; References</h3>]]></content>
      
      <categories>
          
          <category> Windows </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Windows </tag>
            
        </tags>
      
    </entry>
    
    <entry>
      <title>Excel Tips</title>
      <link href="/2018/03/06/excel-tips/"/>
      <content type="html"><![CDATA[<p>Below are some tips for Excel collected during my daily work, which may be helpful for you.</p><h3 id="Add-leading-zeros-to-a-numeric-value"><a href="#Add-leading-zeros-to-a-numeric-value" class="headerlink" title="Add leading zeros to a numeric value"></a>Add leading zeros to a numeric value</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">=TEXT(A1,<span class="string">"0000000000"</span>)</span><br></pre></td></tr></table></figure><h3 id="Add-leading-zeros-to-any-value"><a href="#Add-leading-zeros-to-any-value" class="headerlink" title="Add leading zeros to any value"></a>Add leading zeros to any value</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">=RIGHT(REPT(<span class="string">"0"</span>,10)&amp;A2,10)</span><br></pre></td></tr></table></figure>]]></content>
      
      <categories>
          
          <category> Windows </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Windows </tag>
            
        </tags>
      
    </entry>
    
  
  
</search>
